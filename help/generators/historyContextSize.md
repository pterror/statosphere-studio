# History Context Size

The context limit (in tokens) for this generator's LLM request when Include History is enabled. Set to `0` to use the preset size.

Use this to cap how much history is sent. A value of `2000`–`4000` is usually enough for recap or summary generators without sending the entire conversation.

[Learn more →](https://pterror.github.io/statosphere-guide/syntax/generators#historycontextsize)
