# Minimum Response Tokens

The minimum number of tokens to request in the generated response. Default: `"50"`.

Set this low for short outputs like single words, labels, or yes/no answers. The value is a string (it can reference variables): `"50"`, `"min_length"`.

[Learn more →](https://pterror.github.io/statosphere-guide/syntax/generators#mintokens)
